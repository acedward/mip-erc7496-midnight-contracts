node_modules/
out/
*.log
.DS_Store
.env

# Compiled artefacts: the generated TypeScript/JS, the ZKIR, the compiler
# metadata and the small .verifier keys ARE committed (see README "managed/").
# The .prover keys are megabytes each, are a pure function of the ZKIR, and are
# regenerated by `scripts/compile.sh` — they stay out of git (sub-plan 02 Q20).
*.prover
